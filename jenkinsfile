pipeline {
    agent any

    environment {
        PATH = "/opt/homebrew/bin:${env.PATH}"
    }

    stages {
        stage('Test PATH') {
            steps {
                sh 'echo $PATH'
                sh 'docker --version'
            }
        }
    }
}
